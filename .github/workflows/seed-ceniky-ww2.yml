name: Seed ceníky (2. světová válka)

# 21.5h — jednorázový import komunitních ceníků éry 2. světové války
# (14 ceníků, ~430 položek, dobové USD) + zbraní/výstroje do katalogu Předmětů
# (bez statbloků). Reuse ceniky-import.js (SEED_TAG + ITEM_TAGS). Idempotentní
# dle seedTag; ručně vytvořeného obsahu se nedotkne. BE s polem `currency`
# už je nasazen (21.5g).

on:
  workflow_dispatch:

jobs:
  seed:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup SSH key
        uses: webfactory/ssh-agent@v0.10.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Add server to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -p ${{ vars.SERVER_PORT }} -H ${{ vars.SERVER_HOST }} >> ~/.ssh/known_hosts

      - name: Copy migration files to server
        run: |
          scp -P ${{ vars.SERVER_PORT }} scripts/seed-migrace/ceniky-ww2-seed.json ${{ vars.SSH_USER }}@${{ vars.SERVER_HOST }}:/tmp/ceniky-ww2-seed.json
          scp -P ${{ vars.SERVER_PORT }} scripts/seed-migrace/ceniky-import.js ${{ vars.SSH_USER }}@${{ vars.SERVER_HOST }}:/tmp/ceniky-import.js

      - name: Import into prod Mongo (via BE container)
        run: |
          ssh -o ServerAliveInterval=30 -o ServerAliveCountMax=60 ${{ vars.SSH_USER }}@${{ vars.SERVER_HOST }} -p ${{ vars.SERVER_PORT }} "docker cp /tmp/ceniky-ww2-seed.json projekt-ikaros-be:/tmp/ceniky-ww2-seed.json && docker cp /tmp/ceniky-import.js projekt-ikaros-be:/app/ceniky-import.js && docker exec -w /app -e SEED_JSON=/tmp/ceniky-ww2-seed.json -e SEED_TAG=ceniky:ww2:v1 -e 'ITEM_TAGS=2. světová' projekt-ikaros-be node ceniky-import.js"
