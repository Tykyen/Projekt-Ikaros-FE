/**
 * Spec 1.4 — Audit tab reuse `AuditLogTab` z 1.3b bez funkčních změn
 * (rozhodnutí design-1.4 §12.1 (b) — read-only historie patří mimo
 * Zpracovat queue).
 */
export { AuditLogTab as AuditTab } from '@/features/admin/users/components/AuditLogTab/AuditLogTab';
