/**
 * 10.2d-prep-A — DrD2 diary-pc schéma (typed wrapper kolem JSON).
 * Kostra — plný impl s 8.5 reload.
 */
import schema from './diary-pc.json';
import type { SystemEntitySchema } from '../types';

export const drd2DiaryPcSchema = schema as SystemEntitySchema;
