import { CampaignView } from '@/features/world/campaign';

/**
 * Krok 11.1 — Pavučina (`/svet/:worldSlug/pavucina`).
 * Vztahový graf kampaně + PJ dashboard „Dnes".
 */
export default function CampaignPage() {
  return <CampaignView />;
}
