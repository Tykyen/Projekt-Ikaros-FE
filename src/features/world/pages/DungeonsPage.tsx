/**
 * 21.3a — seznam podzemí světa (`/svet/:worldSlug/podzemi`).
 * Re-export z `dungeon-builder` feature (vzor SoundsPage).
 */
export { default } from '@/features/world/dungeon-builder/components/DungeonListPage';
