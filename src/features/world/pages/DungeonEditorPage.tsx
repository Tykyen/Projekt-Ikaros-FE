/**
 * 21.3a — fullscreen editor podzemí (`/svet/:worldSlug/podzemi/:dungeonId`).
 * Re-export z `dungeon-builder` feature (vzor SoundsPage).
 */
export { default } from '@/features/world/dungeon-builder/components/DungeonEditorPage';
