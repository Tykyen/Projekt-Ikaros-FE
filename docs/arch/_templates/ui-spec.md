# UI Specifikace

## Layout

(Popis rozložení stránky — co je kde, jaká je hierarchie sekcí.)

## Komponenty

(Které UI komponenty jsou potřeba. Odkaž na `src/components/ui/` nebo `features/`.)

| Komponenta | Zdroj | Poznámka |
|------------|-------|----------|
| `Button` | `ui/Button` | (použití) |

## Stavy

### Loading
(Co se zobrazuje během načítání dat.)

### Prázdný stav
(Co se zobrazuje když nejsou žádná data.)

### Chybový stav
(Co se zobrazuje při chybě API.)

## Responsive

| Breakpoint | Chování |
|------------|---------|
| Desktop (>1024px) | (popis) |
| Tablet (769–1024px) | (popis) |
| Mobil (≤768px) | (popis) |

## Interakce

(Popis klíčových uživatelských akcí a jejich výsledků.)
