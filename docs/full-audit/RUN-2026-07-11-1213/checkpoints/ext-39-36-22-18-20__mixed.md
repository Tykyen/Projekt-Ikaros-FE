# ext-39/36/22/18/20 — entitlement · XSS · injection · secret · type-safety

## Styl 39 entitlement:
- chatSkin gate: VYVRÁCENO — chatSkin je záměrně ZDARMA (ne v 3 prémiových výhodách spec 19.4). Nic nepřidávat.
- ⭐ REÁLNÁ díra: `worlds.service.ts:923 approveAccessRequest` NEvolá `assertCanJoinMoreWorlds(žadatel)` → nepodporovatel obejde limit 3 světů přes N× requestAccess → PJ schválí. FIX: přidat assertCanJoinMoreWorlds(ar.userId) před membershipRepo.save (~:937) + do sequential fallback :1018. Test worlds.service.spec (grandfathering!).

## Styl 36 XSS: ČISTÝ — všech 6 prod dangerouslySetInnerHTML má BE sanitize na write-path (timeline:167/210, pages sanitizeTable/CustomData/RichText, jsonLd escaped). Sanitizér striktní allowlist.
- housekeeping: dluhy.md:173 PT-36a + F-RUN-02 už OPRAVENÉ ale vedené jako otevřené → aktualizovat.

## Styl 22 injection: ČISTÝ — escapeRegex() všude (users:296, timeline:54, chat:62, campaign:166, pages:208, sounds anchored). ValidationPipe whitelist+forbidNonWhitelisted → operator injection nejde. ⭐ kosmetika: .slice(0,100) obranně před $regex.

## Styl 18 secret: ČISTÝ — 0 hardcoded secretů, .env necommitnut, CI e-maily maskované, Dependabot ANO, npm audit gate ANO. ⭐ gap: chybí gitleaks/secret-scan v CI → přidat gitleaks-action. ○ set-user-email.yml:55 neuvozený heredoc (admin-only).

## Styl 20 type-safety: ČISTÝ — 0 import type na dekorovaných DTO, 0 as any/@ts-ignore na security cestách. ⭐ volitelně ESLint no-restricted-syntax guard.

## Fix status: approveAccessRequest quota = FIXNU (BE). gitleaks CI + dluhy.md housekeeping + .slice(0,100) = drobné FIXNU. Zbytek čisté.
