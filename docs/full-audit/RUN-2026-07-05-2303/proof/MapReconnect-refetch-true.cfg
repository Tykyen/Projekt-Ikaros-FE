\* Doplňkový běh (proof-only, NENÍ součástí docs/state-consistency-plan/tla/) —
\* stejná spec jako MapReconnect.cfg, jen RefetchOnReconnect = TRUE, aby bylo
\* vidět, že s opravou (refetch on reconnect) Convergence drží. Viz komentář
\* v MapReconnect.tla: „Spusť TLC dvakrát".
SPECIFICATION Spec

CONSTANTS
  MaxOps = 3
  RefetchOnReconnect = TRUE

INVARIANT Safety
INVARIANT Convergence
