# Theme System — Iterace A: Foundation + 3 referenční témata

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Postavit kompletní infrastrukturu globálního theme systému + implementovat tři referenční témata (`modre-nebe`, `bila`, `temna-cerven`) tak, aby switcher dropdown plně fungoval, persistoval do localStorage + (přihlášený) na BE, a všechny existující komponenty respektovaly nové theme tokeny.

**Architecture:** TS theme registry (`src/themes/`) s per-theme `index.ts` (CSS proměnné v JS objektu) + `decorations.css` (lazy-loaded animace/ornamenty). `ThemeProvider` wrappuje aplikaci, `applyTheme(id)` imperativně aplikuje proměnné na layout root + lazy-loadne dekorace + display fonty. Pre-hydration script v `index.html` zabraňuje FOUT. Theme scope na úrovni layoutu (Ikaros/Auth/World) připravuje na fázi 5.0.

**Tech Stack:** React 19, TypeScript, Vite, Jotai (atomWithStorage), Vitest (nově), CSS modules, `sharp` (asset optimize), Storybook 8.

**Spec:** [docs/superpowers/specs/2026-05-07-theme-system-design.md](../specs/2026-05-07-theme-system-design.md)

**Out of scope (Iterace B, samostatný plán):** zbývajících 18 témat (sci-fi, vesmirna-lod, priroda, pergamen, nemrtvi, ctyri-zivly, vesmirna-bitva, hospoda, severske-runy, indiane, africke, arabsky-svet, kyberpunk, postapo, magie, mesic, slunce, zlaty-standard).

---

## Předpoklady před spuštěním

- Pracujeme v repu `c:\Matrix\ProjektIkaros\Projekt-ikaros-FE`
- Branch: vytvoř novou feature branch `feat/theme-system-iterace-a` na začátku
- Backend pro `PATCH /api/users/me` (M6) může být nehotový — FE se chová odolně a tracked je dluh

```bash
git checkout -b feat/theme-system-iterace-a
```

---

## File Structure (vznikne / změní se)

**Nové soubory:**
```
src/themes/
├── types.ts                                     # Theme, ThemeId, ThemeFonts, ThemeScope
├── state.ts                                     # themeAtom (přesun z uiStore.ts)
├── registry.ts                                  # THEMES, DEFAULT_THEME, getTheme, listThemes
├── applyTheme.ts                                # imperativní DOM apply
├── ThemeProvider.tsx                            # context provider + auto-apply on change
├── useTheme.ts                                  # hook { theme, setTheme }
├── useThemeSync.ts                              # BE sync hook (M6)
├── ThemeSwitcher.tsx                            # dropdown komponenta
├── ThemeSwitcher.module.css                     # styly switcheru
├── _shared/
│   ├── tokens.css                               # strukturální tokeny (přesun ze src/index.css)
│   └── reset.css                                # base reset (přesun ze src/index.css)
└── themes/
    ├── modre-nebe/
    │   ├── index.ts
    │   └── decorations.css
    ├── bila/
    │   ├── index.ts
    │   └── decorations.css
    └── temna-cerven/
        ├── index.ts
        └── decorations.css

src/themes/__tests__/
├── registry.test.ts
├── applyTheme.test.ts
├── useTheme.test.tsx
└── useThemeSync.test.tsx

scripts/
└── optimize-theme-assets.mjs                    # PNG → WebP via sharp

public/themes/
├── thumbnails/
│   ├── modre-nebe.webp                          # generated by script
│   ├── bila.webp
│   └── temna-cerven.webp
└── backgrounds/
    └── modre-nebe.webp                          # if PJ dodal PNG

vitest.config.ts                                 # nový — Vitest config
src/__tests__/setup.ts                           # nový — testing-library setup

.storybook/
├── main.ts                                      # Storybook config
└── preview.tsx                                  # global decorators (theme decorator)

src/themes/ThemeSwitcher.stories.tsx             # příklady stories
src/components/ui/Button/Button.stories.tsx      # base komponenty s ThemeDecorator
src/components/ui/Card/Card.stories.tsx
src/components/ui/Input/Input.stories.tsx
```

**Modifikované soubory:**
```
src/main.tsx                                     # ThemeSync → ThemeProvider
src/index.css                                    # smazat placeholder palette + reset přesun
src/store/uiStore.ts                             # smazat activeThemeAtom (přesun do themes/state.ts)
src/components/ThemeSync.tsx                     # smazat (nahrazeno ThemeProvider)
src/components/layout/IkarosLayout/IkarosLayout.tsx       # data-theme wrapper + ThemeSwitcher v headeru
src/components/layout/IkarosLayout/IkarosLayout.module.css # --color-* → nové tokeny
src/components/layout/AuthLayout/AuthLayout.tsx           # data-theme wrapper
src/components/layout/AuthLayout/AuthLayout.module.css    # --color-* → nové tokeny
src/components/layout/WorldLayout/WorldLayout.tsx         # data-theme wrapper
src/components/layout/WorldLayout/WorldLayout.module.css  # --color-* → nové tokeny
src/components/ui/Button/Button.module.css       # --color-* → nové tokeny
src/components/ui/Input/Input.module.css         # --color-* → nové tokeny
src/components/ui/Card/Card.module.css           # --color-* → nové tokeny
src/components/ui/Modal/Modal.module.css         # --color-* → nové tokeny
src/components/ui/Spinner/Spinner.module.css     # --color-* → nové tokeny
src/components/ui/Badge/Badge.module.css         # --color-* → nové tokeny
src/pages/ikaros/DashboardPage.tsx               # 1 výskyt --color-*
index.html                                       # + pre-hydration script + base font preload
package.json                                     # + vitest, sharp, storybook devDeps + skripty
```

---

## Token mapping reference (kompletní)

Tato tabulka platí pro všechny `--color-*` výskyty v migrovaných souborech. Použij přesně toto mapování.

| Stará proměnná | Nová proměnná | Poznámka |
|----------------|---------------|----------|
| `--color-bg` | `--bg-primary` | |
| `--color-bg-elevated` | `--bg-secondary` | sidebary, header |
| `--color-bg-overlay` | `--bg-card-hover` | hover stavy |
| `--color-surface` | `--bg-card` | karty |
| `--color-border` | `--border` | |
| `--color-border-subtle` | `--border-subtle` | jemné dělící linky (volitelný token, vždy definován) |
| `--color-text` | `--text-primary` | |
| `--color-text-muted` | `--text-secondary` | |
| `--color-text-heading` | `--text-primary` | font-weight semibold v komponentě dělá rozdíl |
| `--color-accent` | `--accent` | |
| `--color-accent-hover` | `--accent-bright` | |
| `--color-accent-muted` | `--accent-soft` | nový explicitní token (rgba transparent variant) |
| `--color-accent-border` | `--accent-soft` | sjednoceno (stejné použití) |
| `--color-success` | `--success` | |
| `--color-warning` | `--warning` | |
| `--color-danger` | `--danger` | |
| `--color-info` | `--info` | |
| `--color-online` | `--success` | sémanticky shodné |
| `--color-offline` | `--text-secondary` | sémanticky shodné |

---

# M1: Foundation + modre-nebe

## Task 1: Vytvoř feature branch a setup Vitest

**Files:**
- Create: `vitest.config.ts`
- Create: `src/__tests__/setup.ts`
- Modify: `package.json`

- [ ] **Step 1: Vytvoř branch**

```bash
git checkout main
git pull
git checkout -b feat/theme-system-iterace-a
```

- [ ] **Step 2: Nainstaluj Vitest + testing-library**

```bash
npm i -D vitest @vitest/ui @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom happy-dom
```

- [ ] **Step 3: Vytvoř `vitest.config.ts`**

```ts
/// <reference types="vitest" />
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/__tests__/setup.ts'],
    css: false,
  },
});
```

- [ ] **Step 4: Vytvoř `src/__tests__/setup.ts`**

```ts
import '@testing-library/jest-dom/vitest';
```

- [ ] **Step 5: Přidej skripty do `package.json`**

V sekci `scripts` přidej:
```json
"test": "vitest",
"test:run": "vitest run",
"test:ui": "vitest --ui"
```

- [ ] **Step 6: Smoke test**

Vytvoř dočasný `src/__tests__/smoke.test.ts`:
```ts
import { describe, it, expect } from 'vitest';

describe('smoke', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2);
  });
});
```

Run: `npm run test:run`
Expected: 1 test passed.

- [ ] **Step 7: Smaž smoke test a commit**

```bash
rm src/__tests__/smoke.test.ts
git add vitest.config.ts src/__tests__/setup.ts package.json package-lock.json
git commit -m "chore: setup Vitest + testing-library"
```

---

## Task 2: Definuj theme types

**Files:**
- Create: `src/themes/types.ts`

- [ ] **Step 1: Vytvoř `src/themes/types.ts`**

```ts
export type ThemeId =
  | 'modre-nebe'
  | 'zlaty-standard'
  | 'sci-fi'
  | 'bila'
  | 'vesmirna-lod'
  | 'priroda'
  | 'pergamen'
  | 'nemrtvi'
  | 'ctyri-zivly'
  | 'vesmirna-bitva'
  | 'hospoda'
  | 'severske-runy'
  | 'indiane'
  | 'africke'
  | 'arabsky-svet'
  | 'kyberpunk'
  | 'postapo'
  | 'temna-cerven'
  | 'magie'
  | 'mesic'
  | 'slunce';

export type ThemeScope = 'platform' | 'both';

export type ThemeFonts = {
  display?: string;
  logo?: string;
  body?: string;
};

export type ThemeReducedMotion = 'safe' | 'heavy';

export type Theme = {
  id: ThemeId;
  name: string;
  scope: ThemeScope;
  atmosphere: string;
  vars: Record<string, string>;
  fonts: ThemeFonts;
  thumbnail: string;
  background: string | null;
  decorationsModule: () => Promise<unknown>;
  reducedMotion?: ThemeReducedMotion;
};
```

- [ ] **Step 2: Commit**

```bash
git add src/themes/types.ts
git commit -m "feat(themes): add Theme types"
```

---

## Task 3: Vytvoř _shared/tokens.css (přesun ze src/index.css)

**Files:**
- Create: `src/themes/_shared/tokens.css`
- Create: `src/themes/_shared/reset.css`
- Modify: `src/index.css`

- [ ] **Step 1: Vytvoř `src/themes/_shared/tokens.css`**

```css
/* ── Strukturální tokeny — neměnné napříč tématy ── */

:root {
  /* Spacing */
  --sp-1: 4px;
  --sp-2: 8px;
  --sp-3: 12px;
  --sp-4: 16px;
  --sp-5: 20px;
  --sp-6: 24px;
  --sp-8: 32px;
  --sp-10: 40px;
  --sp-12: 48px;
  --sp-16: 64px;

  /* Typography sizes */
  --text-xs: 11px;
  --text-sm: 13px;
  --text-base: 15px;
  --text-md: 17px;
  --text-lg: 20px;
  --text-xl: 24px;
  --text-2xl: 32px;
  --text-3xl: 42px;

  --leading-tight: 1.2;
  --leading-normal: 1.5;
  --leading-loose: 1.75;

  --weight-normal: 400;
  --weight-medium: 500;
  --weight-semi: 600;
  --weight-bold: 700;

  /* Font families — fallback chain; témata override --font-logo, --font-display */
  --font-sans: system-ui, 'Segoe UI', Roboto, sans-serif;
  --font-mono: ui-monospace, Consolas, 'Courier New', monospace;
  --font-body: 'Lora', Georgia, serif;
  --font-display: 'Cinzel', Georgia, serif;
  --font-logo: 'Cinzel', Georgia, serif;

  /* Border radius */
  --radius-sm: 4px;
  --radius-md: 8px;
  --radius-lg: 12px;
  --radius-xl: 16px;
  --radius-full: 9999px;

  /* Shadows */
  --shadow-sm: 0 1px 3px rgba(0,0,0,.12), 0 1px 2px rgba(0,0,0,.08);
  --shadow-md: 0 4px 12px rgba(0,0,0,.15), 0 2px 4px rgba(0,0,0,.08);
  --shadow-lg: 0 10px 24px rgba(0,0,0,.18), 0 4px 8px rgba(0,0,0,.10);
  --shadow-xl: 0 20px 40px rgba(0,0,0,.22);

  /* Transitions */
  --transition-fast: 100ms ease;
  --transition-base: 180ms ease;
  --transition-slow: 300ms ease;

  /* Layout */
  --header-h: 56px;
  --sidebar-w: 240px;
  --sidebar-w-collapsed: 56px;
  --content-max: 1200px;

  /* Z-index */
  --z-base: 0;
  --z-dropdown: 100;
  --z-sticky: 200;
  --z-overlay: 300;
  --z-modal: 400;
  --z-toast: 500;
}
```

- [ ] **Step 2: Vytvoř `src/themes/_shared/reset.css`**

```css
*, *::before, *::after { box-sizing: border-box; }

html {
  font-family: var(--font-body, var(--font-sans));
  font-size: var(--text-base);
  line-height: var(--leading-normal);
  color: var(--text-primary);
  background: var(--bg-primary);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

body { margin: 0; }

#root {
  min-height: 100svh;
  display: flex;
  flex-direction: column;
}

h1, h2, h3, h4, h5, h6 {
  margin: 0;
  font-family: var(--font-display, var(--font-sans));
  font-weight: var(--weight-semi);
  color: var(--text-primary);
  line-height: var(--leading-tight);
}

p { margin: 0; }

a { color: var(--accent); text-decoration: none; }
a:hover { color: var(--accent-bright); }

button { font-family: inherit; cursor: pointer; }
input, textarea, select { font-family: inherit; }
```

- [ ] **Step 3: Přepiš `src/index.css` jako agregátor**

Smaž celý obsah a nahraď:
```css
/* ────────────────────────────────────────────────────────────
   Ikaros FE — global stylesheet
   Strukturální tokeny + base reset.
   Theme tokens (barvy) přidává ThemeProvider runtime.
──────────────────────────────────────────────────────────── */

@import './themes/_shared/tokens.css';
@import './themes/_shared/reset.css';
```

- [ ] **Step 4: Ověř že aplikace startuje**

```bash
npm run dev
```
Expected: dev server běží na :5173, stránka načte (může vypadat rozbitě bez theme tokens — to je OK, opraví Task 6).

- [ ] **Step 5: Commit**

```bash
git add src/themes/_shared/tokens.css src/themes/_shared/reset.css src/index.css
git commit -m "refactor(themes): split index.css into structural tokens + reset"
```

---

## Task 4: Vytvoř `modre-nebe` theme files

**Files:**
- Create: `src/themes/themes/modre-nebe/index.ts`
- Create: `src/themes/themes/modre-nebe/decorations.css`

- [ ] **Step 1: Vytvoř `src/themes/themes/modre-nebe/index.ts`**

```ts
import type { Theme } from '../../types';

export const modreNebeTheme: Theme = {
  id: 'modre-nebe',
  name: 'Modré nebe',
  scope: 'both',
  atmosphere: 'Heroická fantasy říše pod hvězdnatým modrým nebem',
  vars: {
    '--bg-primary':       '#0a1428',
    '--bg-secondary':     '#101e3c',
    '--bg-card':          '#142850',
    '--bg-card-hover':    '#1c3464',
    '--accent':           '#c89a30',
    '--accent-bright':    '#e8b860',
    '--accent-dim':       '#604814',
    '--accent-soft':      'rgba(200, 154, 48, 0.15)',
    '--text-primary':     '#e8e0c8',
    '--text-secondary':   '#a89870',
    '--text-muted':       '#544830',
    '--border':           '#2a3858',
    '--border-subtle':    '#1c2840',
    '--border-strong':    '#c89a30',
    '--success':          '#3ecf8e',
    '--warning':          '#f5a623',
    '--danger':           '#f06060',
    '--info':             '#5ba4f5',
    '--font-logo':        '"Cinzel Decorative", "Cinzel", Georgia, serif',
    '--font-display':     '"Cinzel", Georgia, serif',
    '--font-body':        '"Lora", Georgia, serif',
  },
  fonts: {
    logo: 'Cinzel Decorative',
    display: 'Cinzel',
    body: 'Lora',
  },
  thumbnail: '/themes/thumbnails/modre-nebe.webp',
  background: '/themes/backgrounds/modre-nebe.webp',
  decorationsModule: () => import('./decorations.css'),
  reducedMotion: 'safe',
};
```

- [ ] **Step 2: Vytvoř `src/themes/themes/modre-nebe/decorations.css`**

```css
/* ── Modré nebe — dekorativní vrstva ──
   Aplikuje se přes [data-theme="modre-nebe"] selektor.
   Animace jsou pod prefers-reduced-motion: no-preference.
   Statické dekor (ornamenty) jsou vždy aktivní.
*/

[data-theme="modre-nebe"] {
  /* Fallback gradient (když background image chybí) */
  background-image:
    radial-gradient(ellipse at 50% 0%, rgba(232, 184, 96, 0.15) 0%, transparent 60%),
    radial-gradient(ellipse at 30% 100%, rgba(20, 40, 80, 0.4) 0%, transparent 60%);
  background-color: var(--bg-primary);
}

/* Diamond ornamentální oddělovače mezi sekcemi */
[data-theme="modre-nebe"] .section-divider::before {
  content: '◆';
  color: var(--accent);
  font-size: 12px;
  margin: 0 var(--sp-3);
}

/* Subtilní hvězdný třpyt na pozadí — jen když uživatel pohyb neomezuje */
@media (prefers-reduced-motion: no-preference) {
  [data-theme="modre-nebe"] {
    /* (placeholder pro future hvězdný třpyt; zatím prázdné) */
  }
}
```

- [ ] **Step 3: Commit**

```bash
git add src/themes/themes/modre-nebe/
git commit -m "feat(themes): add modre-nebe theme files"
```

---

## Task 5: Vytvoř registry s validací (TDD)

**Files:**
- Create: `src/themes/registry.ts`
- Create: `src/themes/__tests__/registry.test.ts`

- [ ] **Step 1: Napiš failing test**

`src/themes/__tests__/registry.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { THEMES, DEFAULT_THEME, getTheme, listThemes } from '../registry';

describe('registry', () => {
  it('exports DEFAULT_THEME = modre-nebe', () => {
    expect(DEFAULT_THEME).toBe('modre-nebe');
  });

  it('THEMES contains modre-nebe entry', () => {
    expect(THEMES['modre-nebe']).toBeDefined();
    expect(THEMES['modre-nebe'].id).toBe('modre-nebe');
  });

  it('getTheme returns the theme for valid id', () => {
    expect(getTheme('modre-nebe').id).toBe('modre-nebe');
  });

  it('getTheme falls back to DEFAULT_THEME for invalid id', () => {
    expect(getTheme('neexistuje').id).toBe(DEFAULT_THEME);
  });

  it('listThemes returns array of all themes', () => {
    const all = listThemes();
    expect(Array.isArray(all)).toBe(true);
    expect(all.length).toBeGreaterThan(0);
    expect(all.every((t) => typeof t.id === 'string')).toBe(true);
  });

  it('listThemes filters by scope', () => {
    const both = listThemes('both');
    expect(both.every((t) => t.scope === 'both')).toBe(true);
  });

  it('every theme has required CSS variables', () => {
    const required = [
      '--bg-primary', '--bg-secondary', '--bg-card', '--bg-card-hover',
      '--accent', '--accent-bright', '--accent-dim',
      '--text-primary', '--text-secondary', '--text-muted',
      '--border', '--border-strong',
      '--success', '--warning', '--danger', '--info',
    ];
    for (const theme of Object.values(THEMES)) {
      for (const key of required) {
        expect(theme.vars[key], `${theme.id} missing ${key}`).toBeDefined();
      }
    }
  });
});
```

- [ ] **Step 2: Run test — verify it fails**

```bash
npm run test:run -- registry.test
```
Expected: FAIL — `Cannot find module '../registry'`.

- [ ] **Step 3: Implement `src/themes/registry.ts`**

```ts
import type { Theme, ThemeId, ThemeScope } from './types';
import { modreNebeTheme } from './themes/modre-nebe';

export const DEFAULT_THEME: ThemeId = 'modre-nebe';

export const THEMES: Partial<Record<ThemeId, Theme>> = {
  'modre-nebe': modreNebeTheme,
};

export function getTheme(id: string): Theme {
  return (THEMES as Record<string, Theme>)[id] ?? THEMES[DEFAULT_THEME]!;
}

export function listThemes(scope?: ThemeScope): Theme[] {
  const all = Object.values(THEMES).filter(Boolean) as Theme[];
  if (!scope) return all;
  return all.filter((t) => t.scope === scope || t.scope === 'both');
}
```

- [ ] **Step 4: Run test — verify it passes**

```bash
npm run test:run -- registry.test
```
Expected: PASS — všech 7 testů.

- [ ] **Step 5: Commit**

```bash
git add src/themes/registry.ts src/themes/__tests__/registry.test.ts
git commit -m "feat(themes): registry with validation tests"
```

---

## Task 6: Implementuj `applyTheme` (TDD)

**Files:**
- Create: `src/themes/applyTheme.ts`
- Create: `src/themes/__tests__/applyTheme.test.ts`

- [ ] **Step 1: Napiš failing test**

`src/themes/__tests__/applyTheme.test.ts`:
```ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { applyTheme } from '../applyTheme';
import { THEMES } from '../registry';

describe('applyTheme', () => {
  beforeEach(() => {
    document.documentElement.removeAttribute('data-theme');
    document.documentElement.style.cssText = '';
    document.head.innerHTML = '';
  });

  it('sets data-theme attribute on html', async () => {
    await applyTheme('modre-nebe');
    expect(document.documentElement.getAttribute('data-theme')).toBe('modre-nebe');
  });

  it('applies all CSS variables from theme.vars', async () => {
    await applyTheme('modre-nebe');
    const theme = THEMES['modre-nebe']!;
    for (const [key, value] of Object.entries(theme.vars)) {
      expect(document.documentElement.style.getPropertyValue(key)).toBe(value);
    }
  });

  it('falls back to default theme for unknown id', async () => {
    await applyTheme('neexistuje');
    expect(document.documentElement.getAttribute('data-theme')).toBe('modre-nebe');
  });

  it('preloads background image when defined', async () => {
    const theme = THEMES['modre-nebe']!;
    const original = theme.background;
    theme.background = '/themes/backgrounds/test.webp';
    const ImageMock = vi.fn(() => ({ src: '' }));
    vi.stubGlobal('Image', ImageMock);
    await applyTheme('modre-nebe');
    expect(ImageMock).toHaveBeenCalled();
    theme.background = original;
    vi.unstubAllGlobals();
  });
});
```

- [ ] **Step 2: Run test — verify it fails**

```bash
npm run test:run -- applyTheme.test
```
Expected: FAIL — `Cannot find module '../applyTheme'`.

- [ ] **Step 3: Implement `src/themes/applyTheme.ts`**

```ts
import { getTheme } from './registry';
import type { ThemeId } from './types';

const loadedFonts = new Set<string>();
const loadedDecorations = new Set<string>();

async function loadDecorations(themeId: ThemeId): Promise<void> {
  if (loadedDecorations.has(themeId)) return;
  const theme = getTheme(themeId);
  await theme.decorationsModule();
  loadedDecorations.add(themeId);
}

async function loadFont(family: string): Promise<void> {
  if (loadedFonts.has(family)) return;
  if (typeof document === 'undefined') return;
  if (document.fonts?.check?.(`16px "${family}"`)) {
    loadedFonts.add(family);
    return;
  }
  const link = document.createElement('link');
  link.rel = 'stylesheet';
  link.href = `https://fonts.googleapis.com/css2?family=${encodeURIComponent(family).replace(/%20/g, '+')}:wght@400;700&display=swap`;
  document.head.appendChild(link);
  try {
    await document.fonts?.load?.(`700 16px "${family}"`);
  } catch {
    /* ignore — font swap fallback by browser */
  }
  loadedFonts.add(family);
}

function preloadBackground(url: string | null): void {
  if (!url || typeof window === 'undefined') return;
  const img = new Image();
  img.src = url;
}

export async function applyTheme(id: string): Promise<void> {
  const theme = getTheme(id);
  document.documentElement.setAttribute('data-theme', theme.id);

  for (const [key, value] of Object.entries(theme.vars)) {
    document.documentElement.style.setProperty(key, value);
  }

  preloadBackground(theme.background);

  const fontPromises: Promise<void>[] = [];
  if (theme.fonts.logo) fontPromises.push(loadFont(theme.fonts.logo));
  if (theme.fonts.display && theme.fonts.display !== theme.fonts.logo) {
    fontPromises.push(loadFont(theme.fonts.display));
  }

  await Promise.all([loadDecorations(theme.id as ThemeId), ...fontPromises]);
}
```

- [ ] **Step 4: Run test — verify it passes**

```bash
npm run test:run -- applyTheme.test
```
Expected: PASS — všechny 4 testy.

- [ ] **Step 5: Commit**

```bash
git add src/themes/applyTheme.ts src/themes/__tests__/applyTheme.test.ts
git commit -m "feat(themes): applyTheme function with tests"
```

---

## Task 7: Přesun atom + state.ts

**Files:**
- Create: `src/themes/state.ts`
- Modify: `src/store/uiStore.ts`

- [ ] **Step 1: Vytvoř `src/themes/state.ts`**

```ts
import { atomWithStorage } from 'jotai/utils';
import { atom } from 'jotai';
import type { ThemeId } from './types';
import { DEFAULT_THEME } from './registry';

export const themeAtom = atomWithStorage<ThemeId>('ikaros.theme', DEFAULT_THEME);
export const worldThemeAtom = atom<ThemeId | null>(null);
```

- [ ] **Step 2: Aktualizuj `src/store/uiStore.ts`**

Smaž `activeThemeAtom` a `worldThemeAtom`:
```ts
import { atomWithStorage } from 'jotai/utils';

export const sidebarOpenAtom = atomWithStorage<boolean>('ikaros.sidebar', true);
```

- [ ] **Step 3: Najdi a oprav importy `activeThemeAtom`**

```bash
grep -rn "activeThemeAtom" src/ --include="*.ts" --include="*.tsx"
```

V každém nalezeném souboru:
- Změň import z `from '../store/uiStore'` na `from '../themes/state'` (uprav cestu dle umístění)
- Změň `activeThemeAtom` na `themeAtom`

Známé výskyty (k aktualizaci v Task 8):
- `src/components/ThemeSync.tsx` — bude smazán v Task 9, ignoruj zatím (nech kompilační error)

- [ ] **Step 4: Commit**

```bash
git add src/themes/state.ts src/store/uiStore.ts
git commit -m "refactor(themes): move theme atoms to themes/state.ts"
```

---

## Task 8: Implementuj `useTheme` hook (TDD)

**Files:**
- Create: `src/themes/useTheme.ts`
- Create: `src/themes/__tests__/useTheme.test.tsx`

- [ ] **Step 1: Napiš failing test**

`src/themes/__tests__/useTheme.test.tsx`:
```tsx
import { describe, it, expect, beforeEach } from 'vitest';
import { renderHook, act } from '@testing-library/react';
import { Provider as JotaiProvider, createStore } from 'jotai';
import { useTheme } from '../useTheme';
import type { PropsWithChildren } from 'react';

function makeWrapper(store = createStore()) {
  return ({ children }: PropsWithChildren) => (
    <JotaiProvider store={store}>{children}</JotaiProvider>
  );
}

describe('useTheme', () => {
  beforeEach(() => {
    localStorage.clear();
    document.documentElement.removeAttribute('data-theme');
  });

  it('returns DEFAULT_THEME initially', () => {
    const { result } = renderHook(() => useTheme(), { wrapper: makeWrapper() });
    expect(result.current.themeId).toBe('modre-nebe');
  });

  it('setTheme updates the atom and applies to DOM', async () => {
    const { result } = renderHook(() => useTheme(), { wrapper: makeWrapper() });
    await act(async () => {
      await result.current.setTheme('modre-nebe');
    });
    expect(result.current.themeId).toBe('modre-nebe');
    expect(document.documentElement.getAttribute('data-theme')).toBe('modre-nebe');
  });

  it('exposes theme metadata', () => {
    const { result } = renderHook(() => useTheme(), { wrapper: makeWrapper() });
    expect(result.current.theme.name).toBe('Modré nebe');
    expect(result.current.theme.id).toBe('modre-nebe');
  });
});
```

- [ ] **Step 2: Run test — verify it fails**

```bash
npm run test:run -- useTheme.test
```
Expected: FAIL — `Cannot find module '../useTheme'`.

- [ ] **Step 3: Implement `src/themes/useTheme.ts`**

```ts
import { useCallback } from 'react';
import { useAtom } from 'jotai';
import { themeAtom } from './state';
import { applyTheme } from './applyTheme';
import { getTheme } from './registry';
import type { ThemeId } from './types';

export function useTheme() {
  const [themeId, setThemeId] = useAtom(themeAtom);

  const setTheme = useCallback(
    async (id: ThemeId) => {
      setThemeId(id);
      await applyTheme(id);
    },
    [setThemeId],
  );

  return {
    themeId,
    theme: getTheme(themeId),
    setTheme,
  };
}
```

- [ ] **Step 4: Run test — verify it passes**

```bash
npm run test:run -- useTheme.test
```
Expected: PASS — 3 testy.

- [ ] **Step 5: Commit**

```bash
git add src/themes/useTheme.ts src/themes/__tests__/useTheme.test.tsx
git commit -m "feat(themes): useTheme hook with tests"
```

---

## Task 9: Implementuj `ThemeProvider` + nahraď `ThemeSync`

**Files:**
- Create: `src/themes/ThemeProvider.tsx`
- Modify: `src/main.tsx`
- Delete: `src/components/ThemeSync.tsx`

- [ ] **Step 1: Vytvoř `src/themes/ThemeProvider.tsx`**

```tsx
import { useEffect, type PropsWithChildren } from 'react';
import { useAtomValue } from 'jotai';
import { themeAtom } from './state';
import { applyTheme } from './applyTheme';

export function ThemeProvider({ children }: PropsWithChildren) {
  const themeId = useAtomValue(themeAtom);

  useEffect(() => {
    void applyTheme(themeId);
  }, [themeId]);

  return <>{children}</>;
}
```

- [ ] **Step 2: Aktualizuj `src/main.tsx`**

Najdi:
```tsx
import { ThemeSync } from "./components/ThemeSync";
```
Nahraď:
```tsx
import { ThemeProvider } from "./themes/ThemeProvider";
```

Najdi:
```tsx
<JotaiProvider>
  <QueryClientProvider client={queryClient}>
    <GlobalErrorBoundary>
      <RouterProvider router={router} />
      <Toaster position="bottom-right" theme="dark" richColors />
    </GlobalErrorBoundary>
  </QueryClientProvider>
  <ThemeSync />
</JotaiProvider>
```

Nahraď:
```tsx
<JotaiProvider>
  <ThemeProvider>
    <QueryClientProvider client={queryClient}>
      <GlobalErrorBoundary>
        <RouterProvider router={router} />
        <Toaster position="bottom-right" theme="dark" richColors />
      </GlobalErrorBoundary>
    </QueryClientProvider>
  </ThemeProvider>
</JotaiProvider>
```

- [ ] **Step 3: Smaž `src/components/ThemeSync.tsx`**

```bash
rm src/components/ThemeSync.tsx
```

- [ ] **Step 4: Ověř kompilaci**

```bash
npm run build
```
Expected: build proběhne bez TypeScript errorů.

- [ ] **Step 5: Commit**

```bash
git add src/themes/ThemeProvider.tsx src/main.tsx
git rm src/components/ThemeSync.tsx
git commit -m "feat(themes): ThemeProvider replaces ThemeSync"
```

---

## Task 10: Pre-hydration script v index.html

**Files:**
- Modify: `index.html`

- [ ] **Step 1: Aktualizuj `index.html`**

Nahraď celý obsah:
```html
<!doctype html>
<html lang="cs">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Projekt Ikaros</title>

    <!-- Pre-hydration: nastaví data-theme před React render → žádný FOUT -->
    <script>
      (function () {
        try {
          var raw = localStorage.getItem('ikaros.theme');
          var id = raw ? JSON.parse(raw) : 'modre-nebe';
          if (typeof id !== 'string') id = 'modre-nebe';
          document.documentElement.setAttribute('data-theme', id);
        } catch (e) {
          document.documentElement.setAttribute('data-theme', 'modre-nebe');
        }
      })();
    </script>

    <!-- Base body fonty — preload (Lora) -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400;0,700;1,400&display=swap"
    />
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

> Poznámka: Lora se zatím načítá z Google Fonts (jednoduchost). Self-host (z `public/fonts/`) lze přidat později jako optimalizaci, pokud bude třeba.

- [ ] **Step 2: Manuální test**

```bash
npm run dev
```
- Otevři `http://localhost:5173`
- DevTools → Elements → ověř že `<html>` má `data-theme="modre-nebe"` HNED při prvním paintu (Network → throttle Slow 3G a refresh; nikdy nesmí být briefly bez atributu)

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "feat(themes): pre-hydration data-theme script + Lora preload"
```

---

## Task 11: Wrap layouty data-theme

**Files:**
- Modify: `src/components/layout/IkarosLayout/IkarosLayout.tsx`
- Modify: `src/components/layout/AuthLayout/AuthLayout.tsx`
- Modify: `src/components/layout/WorldLayout/WorldLayout.tsx`

- [ ] **Step 1: Aktualizuj `IkarosLayout.tsx`**

Najdi:
```tsx
import { useState } from 'react';
import { NavLink, Outlet, Link } from 'react-router-dom';
```
Přidej za to:
```tsx
import { useAtomValue } from 'jotai';
import { themeAtom } from '../../../themes/state';
import { getTheme } from '../../../themes/registry';
```

(pokud `useAtomValue` už importováno, přidej jen zbylé importy)

V `export function IkarosLayout()` najdi:
```tsx
return (
  <div className={s.shell}>
```

Nahraď:
```tsx
const themeId = useAtomValue(themeAtom);
const theme = getTheme(themeId);
const bgStyle = theme.background
  ? { backgroundImage: `url(${theme.background})`, backgroundSize: 'cover', backgroundPosition: 'center', backgroundAttachment: 'fixed' as const }
  : undefined;

return (
  <div className={s.shell} data-theme={themeId} style={bgStyle}>
```

- [ ] **Step 2: Aktualizuj `AuthLayout.tsx`**

Otevři `src/components/layout/AuthLayout/AuthLayout.tsx` a aplikuj stejnou logiku:
```tsx
import { useAtomValue } from 'jotai';
import { themeAtom } from '../../../themes/state';
import { getTheme } from '../../../themes/registry';

export function AuthLayout() {
  const themeId = useAtomValue(themeAtom);
  const theme = getTheme(themeId);
  const bgStyle = theme.background
    ? { backgroundImage: `url(${theme.background})`, backgroundSize: 'cover', backgroundPosition: 'center', backgroundAttachment: 'fixed' as const }
    : undefined;

  // ... existing logic ...
  return (
    <div data-theme={themeId} style={bgStyle} /* keep existing className */>
      {/* existing JSX */}
    </div>
  );
}
```

(Existing JSX nechat beze změny, jen přidat data-theme + style na nejvyšší element.)

- [ ] **Step 3: Aktualizuj `WorldLayout.tsx`**

Stejný postup jako AuthLayout — přidat `useAtomValue(themeAtom)` + `getTheme()` + `data-theme={themeId}` + `style={bgStyle}` na nejvyšší element.

> V Iteraci A `WorldLayout` používá `themeAtom` (uživatelův theme). V budoucí fázi 5.0 se nahradí logikou `getTheme(world.themeId ?? userThemeId)`.

- [ ] **Step 4: Manuální test**

```bash
npm run dev
```
Otevři aplikaci → DevTools → Elements → najdi nejvyšší layout div → ověř že má `data-theme="modre-nebe"`. Změň ručně v DevTools na něco jiného a zkontroluj že `<html data-theme>` zůstane (kvůli pre-hydration), ale layout div se změní (kvůli React rerender).

- [ ] **Step 5: Commit**

```bash
git add src/components/layout/
git commit -m "feat(themes): scope data-theme to layout root (M5.0 ready)"
```

---

## Task 12: Aktualizuj `_shared/reset.css` aby používal nové tokeny

**Files:**
- Modify: `src/themes/_shared/reset.css`

(Reset.css už používá `var(--text-primary)` atd. v Tasku 3 — tady to ověříme a doděláme případné chybějící části.)

- [ ] **Step 1: Verify reset.css je správné**

```bash
cat src/themes/_shared/reset.css | grep "var(--"
```
Expected: výstup obsahuje pouze `var(--bg-primary)`, `var(--text-primary)`, `var(--accent)`, `var(--accent-bright)`, `var(--font-body)`, `var(--font-display)`, `var(--font-sans)`. Žádné `var(--color-*)`.

- [ ] **Step 2: Pokud najdeš `var(--color-*)`, oprav podle [token mappingu](#token-mapping-reference-kompletní)**

- [ ] **Step 3: Commit (skip pokud nic se nezměnilo)**

---

## Task 13: M1 manuální verifikace + commit checkpoint

- [ ] **Step 1: Run dev server**

```bash
npm run dev
```

- [ ] **Step 2: Otevři DevTools Console**

V konzoli spusť:
```js
// 1. Změna tématu funguje
document.documentElement.setAttribute('data-theme', 'modre-nebe');
// → barvy se aplikují (i když kommerční komponenty pořád referencují --color-*, header by měl ukázat změnu)

// 2. CSS variables se nastavily
getComputedStyle(document.documentElement).getPropertyValue('--bg-primary');
// → '#0a1428' (bez bílého space)
```

- [ ] **Step 3: Run all tests**

```bash
npm run test:run
```
Expected: všechny testy projdou (registry, applyTheme, useTheme).

- [ ] **Step 4: Run build**

```bash
npm run build
```
Expected: build success bez TypeScript errorů.

- [ ] **Step 5: M1 checkpoint commit (pokud cokoli zbylo)**

```bash
git status
# pokud nic, skip; jinak:
git commit -am "chore(themes): M1 checkpoint — foundation + modre-nebe"
```

---

# M2: ThemeSwitcher UI

## Task 14: ThemeSwitcher skeleton + trigger

**Files:**
- Create: `src/themes/ThemeSwitcher.tsx`
- Create: `src/themes/ThemeSwitcher.module.css`

- [ ] **Step 1: Vytvoř `src/themes/ThemeSwitcher.tsx`**

```tsx
import { useState, useRef, useEffect } from 'react';
import { useTheme } from './useTheme';
import { listThemes } from './registry';
import type { ThemeId } from './types';
import s from './ThemeSwitcher.module.css';

export function ThemeSwitcher() {
  const { themeId, theme, setTheme } = useTheme();
  const [open, setOpen] = useState(false);
  const wrapperRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    if (!open) return;
    const onClickOutside = (e: MouseEvent) => {
      if (wrapperRef.current && !wrapperRef.current.contains(e.target as Node)) {
        setOpen(false);
      }
    };
    const onEsc = (e: KeyboardEvent) => {
      if (e.key === 'Escape') setOpen(false);
    };
    document.addEventListener('mousedown', onClickOutside);
    document.addEventListener('keydown', onEsc);
    return () => {
      document.removeEventListener('mousedown', onClickOutside);
      document.removeEventListener('keydown', onEsc);
    };
  }, [open]);

  const handleSelect = async (id: ThemeId) => {
    await setTheme(id);
    setOpen(false);
  };

  const themes = listThemes();

  return (
    <div ref={wrapperRef} className={s.wrapper}>
      <button
        type="button"
        className={s.trigger}
        onClick={() => setOpen((o) => !o)}
        aria-haspopup="listbox"
        aria-expanded={open}
        aria-label="Vybrat motiv aplikace"
      >
        <img src={theme.thumbnail} alt="" className={s.triggerThumb} />
        <span className={s.triggerName}>{theme.name}</span>
        <span className={s.triggerCaret} aria-hidden="true">▼</span>
      </button>

      {open && (
        <ul className={s.popover} role="listbox" aria-label="Motivy aplikace">
          {themes.map((t) => (
            <li key={t.id}>
              <button
                type="button"
                className={`${s.option} ${t.id === themeId ? s.optionActive : ''}`}
                onClick={() => void handleSelect(t.id)}
                role="option"
                aria-selected={t.id === themeId}
              >
                <img src={t.thumbnail} alt="" className={s.optionThumb} />
                <span className={s.optionName}>{t.name}</span>
                {t.id === themeId && <span className={s.optionCheck} aria-hidden="true">✓</span>}
              </button>
            </li>
          ))}
        </ul>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Vytvoř `src/themes/ThemeSwitcher.module.css`**

```css
.wrapper {
  position: relative;
  display: inline-block;
}

.trigger {
  display: inline-flex;
  align-items: center;
  gap: var(--sp-2);
  padding: var(--sp-1) var(--sp-3);
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  color: var(--text-primary);
  font-size: var(--text-sm);
  font-weight: var(--weight-medium);
  font-family: var(--font-body);
  cursor: pointer;
  transition: background var(--transition-fast), border-color var(--transition-fast);
}
.trigger:hover {
  background: var(--bg-card-hover);
  border-color: var(--accent);
}

.triggerThumb {
  width: 24px;
  height: 24px;
  border-radius: var(--radius-sm);
  object-fit: cover;
  flex-shrink: 0;
}

.triggerName {
  text-transform: uppercase;
  letter-spacing: 0.04em;
  white-space: nowrap;
}

.triggerCaret {
  font-size: 10px;
  color: var(--text-secondary);
}

.popover {
  position: absolute;
  top: calc(100% + var(--sp-1));
  right: 0;
  min-width: 280px;
  max-height: min(520px, 70vh);
  overflow-y: auto;
  background: var(--bg-secondary);
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-lg);
  list-style: none;
  margin: 0;
  padding: var(--sp-1);
  z-index: var(--z-dropdown);
}

.option {
  display: flex;
  align-items: center;
  gap: var(--sp-3);
  width: 100%;
  padding: var(--sp-2) var(--sp-3);
  background: transparent;
  border: none;
  border-radius: var(--radius-sm);
  color: var(--text-primary);
  font-size: var(--text-sm);
  font-family: var(--font-body);
  text-align: left;
  cursor: pointer;
  transition: background var(--transition-fast);
}
.option:hover, .option:focus-visible {
  background: var(--bg-card-hover);
  outline: none;
}
.optionActive {
  background: var(--accent-soft);
}

.optionThumb {
  width: 64px;
  height: 40px;
  border-radius: var(--radius-sm);
  object-fit: cover;
  flex-shrink: 0;
}

.optionName {
  flex: 1;
  text-transform: uppercase;
  letter-spacing: 0.04em;
}

.optionCheck {
  color: var(--accent);
  font-weight: var(--weight-bold);
}

/* ── Mobile bottom-sheet ── */
@media (max-width: 768px) {
  .popover {
    position: fixed;
    top: auto;
    bottom: 0;
    left: 0;
    right: 0;
    max-height: 70vh;
    border-radius: var(--radius-lg) var(--radius-lg) 0 0;
    padding: var(--sp-3);
  }
  .option {
    padding: var(--sp-3);
    min-height: 60px;
  }
}
```

- [ ] **Step 3: Commit**

```bash
git add src/themes/ThemeSwitcher.tsx src/themes/ThemeSwitcher.module.css
git commit -m "feat(themes): ThemeSwitcher component (skeleton)"
```

---

## Task 15: Klávesová navigace v ThemeSwitcheru

**Files:**
- Modify: `src/themes/ThemeSwitcher.tsx`

- [ ] **Step 1: Aktualizuj `ThemeSwitcher.tsx` o keyboard handling**

V import sekci přidej:
```tsx
import { useState, useRef, useEffect, type KeyboardEvent } from 'react';
```

V komponentě přidej focus index state:
```tsx
const [focusIndex, setFocusIndex] = useState(0);
```

Přidej helper:
```tsx
const onKeyDown = (e: KeyboardEvent<HTMLUListElement>) => {
  if (e.key === 'ArrowDown') {
    e.preventDefault();
    setFocusIndex((i) => Math.min(i + 1, themes.length - 1));
  } else if (e.key === 'ArrowUp') {
    e.preventDefault();
    setFocusIndex((i) => Math.max(i - 1, 0));
  } else if (e.key === 'Home') {
    e.preventDefault();
    setFocusIndex(0);
  } else if (e.key === 'End') {
    e.preventDefault();
    setFocusIndex(themes.length - 1);
  } else if (e.key === 'Enter' || e.key === ' ') {
    e.preventDefault();
    const t = themes[focusIndex];
    if (t) void handleSelect(t.id);
  }
};
```

V `<ul className={s.popover}>` přidej:
```tsx
<ul
  className={s.popover}
  role="listbox"
  aria-label="Motivy aplikace"
  onKeyDown={onKeyDown}
  tabIndex={-1}
>
```

V `<li>` mapování změň aby reflektoval focus:
```tsx
{themes.map((t, idx) => (
  <li key={t.id}>
    <button
      type="button"
      className={`${s.option} ${t.id === themeId ? s.optionActive : ''} ${idx === focusIndex ? s.optionFocused : ''}`}
      onClick={() => void handleSelect(t.id)}
      role="option"
      aria-selected={t.id === themeId}
      data-index={idx}
    >
      ...
    </button>
  </li>
))}
```

V `useEffect` na open, po `setOpen(true)` přidej focus na popover. Přidej nový ref:
```tsx
const popoverRef = useRef<HTMLUListElement>(null);

useEffect(() => {
  if (open) {
    setFocusIndex(themes.findIndex((t) => t.id === themeId));
    popoverRef.current?.focus();
  }
}, [open, themeId, themes]);
```

A přidej `ref={popoverRef}` do `<ul>`.

- [ ] **Step 2: Přidej `optionFocused` styl do CSS**

V `src/themes/ThemeSwitcher.module.css` přidej:
```css
.optionFocused {
  background: var(--bg-card-hover);
  outline: 2px solid var(--accent);
  outline-offset: -2px;
}
```

- [ ] **Step 3: Commit**

```bash
git add src/themes/ThemeSwitcher.tsx src/themes/ThemeSwitcher.module.css
git commit -m "feat(themes): keyboard navigation in ThemeSwitcher"
```

---

## Task 16: Wire ThemeSwitcher do IkarosLayout headeru

**Files:**
- Modify: `src/components/layout/IkarosLayout/IkarosLayout.tsx`

- [ ] **Step 1: Import ThemeSwitcher**

V `IkarosLayout.tsx` přidej:
```tsx
import { ThemeSwitcher } from '../../../themes/ThemeSwitcher';
```

- [ ] **Step 2: Přidej do `headerNav`**

Najdi:
```tsx
<nav className={s.headerNav}>
  <NavLink
    to="/ikaros/posta"
    ...
```

Přidej PŘED `NavLink` na poštu:
```tsx
<nav className={s.headerNav}>
  <ThemeSwitcher />
  <NavLink
    to="/ikaros/posta"
```

- [ ] **Step 3: Manuální test**

```bash
npm run dev
```
- Otevři aplikaci
- V hlavičce vlevo od "Pošta" je ThemeSwitcher s thumbnailem (zatím chybí — neuložený) a textem "MODRÉ NEBE"
- Klik → otevře se popover se seznamem (zatím jen 1 položka)

> Thumbnail bude broken image dokud nespustíme optimize skript v Tasku 22.

- [ ] **Step 4: Commit**

```bash
git add src/components/layout/IkarosLayout/IkarosLayout.tsx
git commit -m "feat(themes): wire ThemeSwitcher into IkarosLayout header"
```

---

# M3: Asset pipeline + bila + temna-cerven

## Task 17: Setup `sharp` + optimize script

**Files:**
- Create: `scripts/optimize-theme-assets.mjs`
- Modify: `package.json`

- [ ] **Step 1: Nainstaluj sharp**

```bash
npm i -D sharp
```

- [ ] **Step 2: Vytvoř `scripts/optimize-theme-assets.mjs`**

```js
import { readdir, mkdir, stat } from 'node:fs/promises';
import { existsSync } from 'node:fs';
import path from 'node:path';
import sharp from 'sharp';

const SRC_BASE = path.resolve('docs/themes/assets');
const OUT_BASE = path.resolve('public/themes');

// Thumbnaily čteme z root assets/ (současné mockupy s diakritikou).
// Backgrounds čteme z assets/backgrounds/ (slugified PNGs dodané PJ).
const TARGETS = [
  { src: '.',           out: 'thumbnails',  width: 320,  height: 180,  quality: 82 },
  { src: 'backgrounds', out: 'backgrounds', width: 1920, height: 1080, quality: 80 },
];

// Mapping diakritiky / mezer / překlepů → ASCII slug (theme ID).
const FILENAME_MAP = {
  'Modré nebe.png': 'modre-nebe.png',
  'Zlatý standart.png': 'zlaty-standard.png',
  'Sci-fi.png': 'sci-fi.png',
  'Bílá.png': 'bila.png',
  'Vesmírná loď.png': 'vesmirna-lod.png',
  'Příroda.png': 'priroda.png',
  'Pergamen.png': 'pergamen.png',
  'Nemrtví.png': 'nemrtvi.png',
  'Čtyři živly.png': 'ctyri-zivly.png',
  'Vesmírná bitva.png': 'vesmirna-bitva.png',
  'Hospoda.png': 'hospoda.png',
  'Severské runy.png': 'severske-runy.png',
  'Indiáni.png': 'indiane.png',
  'Afrika.png': 'africke.png',
  'Arábie.png': 'arabsky-svet.png',
  'Kyberpunk.png': 'kyberpunk.png',
  'Apokalypsa.png': 'postapo.png',
  'Temná červeň.png': 'temna-cerven.png',
  'Magie.png': 'magie.png',
  'Měsíc.png': 'mesic.png',
  'Slunce.png': 'slunce.png',
};

function slugify(filename) {
  return FILENAME_MAP[filename] ?? filename.toLowerCase();
}

async function processDir(target) {
  const { src, out, width, height, quality } = target;
  const srcDir = path.join(SRC_BASE, src);
  const outDir = path.join(OUT_BASE, out);

  if (!existsSync(srcDir)) {
    console.warn(`[themes] ${srcDir} neexistuje, skip`);
    return;
  }

  await mkdir(outDir, { recursive: true });

  // withFileTypes: true → odfiltruje subdirectories (důležité pro src: '.' aby nezpracoval backgrounds/)
  const entries = await readdir(srcDir, { withFileTypes: true });
  const pngs = entries
    .filter((e) => e.isFile() && /\.png$/i.test(e.name))
    .map((e) => e.name);

  for (const file of pngs) {
    const srcPath = path.join(srcDir, file);
    const outName = slugify(file).replace(/\.png$/i, '.webp');
    const outPath = path.join(outDir, outName);

    try {
      await sharp(srcPath)
        .resize(width, height, { fit: 'cover', position: 'center', withoutEnlargement: true })
        .webp({ quality })
        .toFile(outPath);
      const stats = await stat(outPath);
      console.log(`✓ ${file} → ${outName} (${(stats.size / 1024).toFixed(0)} KB)`);
    } catch (err) {
      console.error(`✗ ${file}: ${err.message}`);
    }
  }
}

async function main() {
  console.log('[themes] Optimizing assets...');
  for (const target of TARGETS) {
    await processDir(target);
  }
  console.log('[themes] Done.');
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
```

- [ ] **Step 3: Přidej skript do `package.json`**

```json
"themes:optimize": "node scripts/optimize-theme-assets.mjs"
```

- [ ] **Step 4: Spusť skript**

```bash
npm run themes:optimize
```
Expected: vygeneruje `public/themes/thumbnails/<id>.webp` pro všech 21 témat (ze současných mockupů). Pro `backgrounds/` zatím 0 nebo málo souborů (PJ je dodává).

- [ ] **Step 5: Ověř výstup**

```bash
ls public/themes/thumbnails/
# Měl bys vidět: modre-nebe.webp, zlaty-standard.webp, ..., slunce.webp
```

- [ ] **Step 6: Commit (vč. generated assets do gitu nebo .gitignore — uživatel rozhodne)**

```bash
git add scripts/optimize-theme-assets.mjs package.json package-lock.json public/themes/
git commit -m "feat(themes): asset optimize script + generate WebP thumbnails"
```

> **Pozn:** Pokud chceš, aby `public/themes/` nebylo v gitu (regenerace at build), přidej do `.gitignore` řádek `public/themes/` a místo toho přidej `prebuild` skript do package.json: `"prebuild": "npm run themes:optimize"`. Vyber preferenci.

---

## Task 18: Bila theme files

**Files:**
- Create: `src/themes/themes/bila/index.ts`
- Create: `src/themes/themes/bila/decorations.css`

- [ ] **Step 1: Vytvoř `src/themes/themes/bila/index.ts`**

```ts
import type { Theme } from '../../types';

export const bilaTheme: Theme = {
  id: 'bila',
  name: 'Bílá',
  scope: 'both',
  atmosphere: 'Krémová nebeská čistota — jediné světlé téma kolekce',
  vars: {
    '--bg-primary':       '#fbf6e9',
    '--bg-secondary':     '#f4ecd8',
    '--bg-card':          '#ffffff',
    '--bg-card-hover':    '#f8f1de',
    '--accent':           '#c4a057',
    '--accent-bright':    '#d4b878',
    '--accent-dim':       '#806530',
    '--accent-soft':      'rgba(196, 160, 87, 0.18)',
    '--text-primary':     '#3a2e1e',
    '--text-secondary':   '#7a6a4e',
    '--text-muted':       '#aa9a7e',
    '--border':           '#d8cca8',
    '--border-subtle':    '#e8dcb8',
    '--border-strong':    '#c4a057',
    '--success':          '#3a8a4e',
    '--warning':          '#c87820',
    '--danger':           '#a83838',
    '--info':             '#3060a0',
    '--font-logo':        '"Cinzel", Georgia, serif',
    '--font-display':     '"Cinzel", Georgia, serif',
    '--font-body':        '"Lora", Georgia, serif',
  },
  fonts: {
    logo: 'Cinzel',
    display: 'Cinzel',
    body: 'Lora',
  },
  thumbnail: '/themes/thumbnails/bila.webp',
  background: '/themes/backgrounds/bila.webp',
  decorationsModule: () => import('./decorations.css'),
  reducedMotion: 'safe',
};
```

- [ ] **Step 2: Vytvoř `src/themes/themes/bila/decorations.css`**

```css
/* ── Bílá — světlé téma, picture-frame karty ── */

[data-theme="bila"] {
  background-image:
    radial-gradient(ellipse at 50% 0%, rgba(212, 184, 120, 0.15) 0%, transparent 60%),
    linear-gradient(to bottom, #fbf6e9 0%, #f4ecd8 100%);
  background-color: var(--bg-primary);
  color-scheme: light;
}

/* Picture-frame styl pro karty */
[data-theme="bila"] .card,
[data-theme="bila"] [class*="card"] {
  border: 1px solid var(--border);
  box-shadow: var(--shadow-sm);
}
```

- [ ] **Step 3: Commit**

```bash
git add src/themes/themes/bila/
git commit -m "feat(themes): add bila theme (light)"
```

---

## Task 19: Temna-cerven theme files

**Files:**
- Create: `src/themes/themes/temna-cerven/index.ts`
- Create: `src/themes/themes/temna-cerven/decorations.css`

- [ ] **Step 1: Vytvoř `src/themes/themes/temna-cerven/index.ts`**

```ts
import type { Theme } from '../../types';

export const temnaCervenTheme: Theme = {
  id: 'temna-cerven',
  name: 'Temná červeň',
  scope: 'both',
  atmosphere: 'Gotická katedrála upíří šlechty — krev a černá, nic jiného',
  vars: {
    '--bg-primary':       '#080404',
    '--bg-secondary':     '#100608',
    '--bg-card':          '#140808',
    '--bg-card-hover':    '#1c0c0c',
    '--accent':           '#8a0010',
    '--accent-bright':    '#c00018',
    '--accent-dim':       '#400008',
    '--accent-soft':      'rgba(138, 0, 16, 0.20)',
    '--text-primary':     '#d0a0a0',
    '--text-secondary':   '#704040',
    '--text-muted':       '#301818',
    '--border':           '#200808',
    '--border-subtle':    '#180404',
    '--border-strong':    '#8a0010',
    '--success':          '#3ecf8e',
    '--warning':          '#f5a623',
    '--danger':           '#e02030',
    '--info':             '#5ba4f5',
    '--font-logo':        '"MedievalSharp", "Cinzel", Georgia, serif',
    '--font-display':     '"IM Fell English", "Cinzel", Georgia, serif',
    '--font-body':        '"Lora", Georgia, serif',
  },
  fonts: {
    logo: 'MedievalSharp',
    display: 'IM Fell English',
    body: 'Lora',
  },
  thumbnail: '/themes/thumbnails/temna-cerven.webp',
  background: '/themes/backgrounds/temna-cerven.webp',
  decorationsModule: () => import('./decorations.css'),
  reducedMotion: 'heavy',
};
```

- [ ] **Step 2: Vytvoř `src/themes/themes/temna-cerven/decorations.css`**

```css
/* ── Temná červeň — gotická katedrála ── */

[data-theme="temna-cerven"] {
  background-image:
    radial-gradient(ellipse at 50% 30%, rgba(192, 0, 24, 0.12) 0%, transparent 50%),
    radial-gradient(ellipse at 50% 100%, rgba(64, 0, 8, 0.6) 0%, transparent 70%);
  background-color: var(--bg-primary);
}

/* Téměř pravoúhlé karty s krevní bordurou */
[data-theme="temna-cerven"] .card,
[data-theme="temna-cerven"] [class*="card"] {
  border-radius: 3px;
  border: 1px solid var(--border-strong);
}

/* Krevní hover glow — animace pod motion-safe */
@media (prefers-reduced-motion: no-preference) {
  [data-theme="temna-cerven"] button:hover {
    box-shadow: 0 0 12px rgba(138, 0, 16, 0.6);
  }
}
```

- [ ] **Step 3: Commit**

```bash
git add src/themes/themes/temna-cerven/
git commit -m "feat(themes): add temna-cerven theme (gothic vampire)"
```

---

## Task 20: Registrace bila + temna-cerven do registry

**Files:**
- Modify: `src/themes/registry.ts`

- [ ] **Step 1: Aktualizuj registry**

```ts
import type { Theme, ThemeId, ThemeScope } from './types';
import { modreNebeTheme } from './themes/modre-nebe';
import { bilaTheme } from './themes/bila';
import { temnaCervenTheme } from './themes/temna-cerven';

export const DEFAULT_THEME: ThemeId = 'modre-nebe';

export const THEMES: Partial<Record<ThemeId, Theme>> = {
  'modre-nebe':   modreNebeTheme,
  'bila':         bilaTheme,
  'temna-cerven': temnaCervenTheme,
};

export function getTheme(id: string): Theme {
  return (THEMES as Record<string, Theme>)[id] ?? THEMES[DEFAULT_THEME]!;
}

export function listThemes(scope?: ThemeScope): Theme[] {
  const all = Object.values(THEMES).filter(Boolean) as Theme[];
  if (!scope) return all;
  return all.filter((t) => t.scope === scope || t.scope === 'both');
}
```

- [ ] **Step 2: Run testy**

```bash
npm run test:run
```
Expected: registry.test passes (`every theme has required CSS variables` test ověří že obě nová témata mají kompletní vars).

- [ ] **Step 3: Manuální test ve switcheru**

```bash
npm run dev
```
- Otevři aplikaci, klikni na ThemeSwitcher v hlavičce
- V seznamu vidíš: MODRÉ NEBE, BÍLÁ, TEMNÁ ČERVEŇ
- Klikni na BÍLÁ → barvy se změní na světlé
- Klikni na TEMNÁ ČERVEŇ → barvy se změní na krev/černá
- Refresh stránky — uložené téma se načte (žádný FOUT)

- [ ] **Step 4: Commit**

```bash
git add src/themes/registry.ts
git commit -m "feat(themes): register bila + temna-cerven"
```

---

# M4: Migrace komponent na nové tokeny

> **Strategie:** každý CSS modul migrujeme zvlášť (1 task = 1 soubor). Použij token mapping table z hlavičky plánu. Po každé migraci spusť `npm run dev` a ručně zkontroluj že komponenta vypadá v `modre-nebe` rozumně (nemusí být pixel-perfect — jen funkční).

## Task 21: Migrace `IkarosLayout.module.css`

**Files:**
- Modify: `src/components/layout/IkarosLayout/IkarosLayout.module.css`

- [ ] **Step 1: Najdi všechny `--color-*` výskyty**

```bash
grep -n "var(--color-" src/components/layout/IkarosLayout/IkarosLayout.module.css
```

- [ ] **Step 2: Aplikuj token mapping**

V souboru proveď find-replace dle [token mapping reference](#token-mapping-reference-kompletní). Konkrétně:

| Najdi | Nahraď |
|-------|--------|
| `var(--color-bg)` | `var(--bg-primary)` |
| `var(--color-bg-elevated)` | `var(--bg-secondary)` |
| `var(--color-bg-overlay)` | `var(--bg-card-hover)` |
| `var(--color-border)` | `var(--border)` |
| `var(--color-border-subtle)` | `var(--border-subtle)` |
| `var(--color-text)` | `var(--text-primary)` |
| `var(--color-text-muted)` | `var(--text-secondary)` |
| `var(--color-text-heading)` | `var(--text-primary)` |
| `var(--color-accent)` | `var(--accent)` |
| `var(--color-accent-hover)` | `var(--accent-bright)` |
| `var(--color-accent-muted)` | `var(--accent-soft)` |
| `var(--color-accent-border)` | `var(--accent-soft)` |
| `var(--color-danger)` | `var(--danger)` |
| `var(--color-online)` | `var(--success)` |

- [ ] **Step 3: Verify žádný `--color-*` nezbyl**

```bash
grep -n "var(--color-" src/components/layout/IkarosLayout/IkarosLayout.module.css
```
Expected: žádný výstup.

- [ ] **Step 4: Manuální test**

```bash
npm run dev
```
- Otevři `/` a ověř že hlavička, sidebar, right panel mají rozumné barvy

- [ ] **Step 5: Commit**

```bash
git add src/components/layout/IkarosLayout/IkarosLayout.module.css
git commit -m "refactor(layout): migrate IkarosLayout to theme tokens"
```

---

## Task 22: Migrace `AuthLayout.module.css`

**Files:**
- Modify: `src/components/layout/AuthLayout/AuthLayout.module.css`

- [ ] **Step 1-3: Stejný postup jako Task 21**

```bash
grep -n "var(--color-" src/components/layout/AuthLayout/AuthLayout.module.css
```
Aplikuj token mapping, verify, commit:

```bash
git add src/components/layout/AuthLayout/AuthLayout.module.css
git commit -m "refactor(layout): migrate AuthLayout to theme tokens"
```

---

## Task 23: Migrace `WorldLayout.module.css`

**Files:**
- Modify: `src/components/layout/WorldLayout/WorldLayout.module.css`

- [ ] **Step 1-3: Stejný postup**

```bash
grep -n "var(--color-" src/components/layout/WorldLayout/WorldLayout.module.css
```
Aplikuj mapping, verify, commit:

```bash
git add src/components/layout/WorldLayout/WorldLayout.module.css
git commit -m "refactor(layout): migrate WorldLayout to theme tokens"
```

---

## Task 24: Migrace `Button.module.css`

**Files:**
- Modify: `src/components/ui/Button/Button.module.css`

- [ ] **Step 1-3: Stejný postup**

```bash
grep -n "var(--color-" src/components/ui/Button/Button.module.css
```
Aplikuj mapping, verify, commit:

```bash
git add src/components/ui/Button/Button.module.css
git commit -m "refactor(ui): migrate Button to theme tokens"
```

---

## Task 25: Migrace `Input.module.css`

**Files:**
- Modify: `src/components/ui/Input/Input.module.css`

- [ ] **Step 1-3: Stejný postup**

```bash
git add src/components/ui/Input/Input.module.css
git commit -m "refactor(ui): migrate Input to theme tokens"
```

---

## Task 26: Migrace `Card.module.css`

**Files:**
- Modify: `src/components/ui/Card/Card.module.css`

- [ ] **Step 1-3: Stejný postup**

```bash
git add src/components/ui/Card/Card.module.css
git commit -m "refactor(ui): migrate Card to theme tokens"
```

---

## Task 27: Migrace `Modal.module.css`

**Files:**
- Modify: `src/components/ui/Modal/Modal.module.css`

- [ ] **Step 1-3: Stejný postup**

```bash
git add src/components/ui/Modal/Modal.module.css
git commit -m "refactor(ui): migrate Modal to theme tokens"
```

---

## Task 28: Migrace `Spinner.module.css`

**Files:**
- Modify: `src/components/ui/Spinner/Spinner.module.css`

- [ ] **Step 1-3: Stejný postup**

```bash
git add src/components/ui/Spinner/Spinner.module.css
git commit -m "refactor(ui): migrate Spinner to theme tokens"
```

---

## Task 29: Migrace `Badge.module.css`

**Files:**
- Modify: `src/components/ui/Badge/Badge.module.css`

- [ ] **Step 1-3: Stejný postup**

```bash
git add src/components/ui/Badge/Badge.module.css
git commit -m "refactor(ui): migrate Badge to theme tokens"
```

---

## Task 30: Migrace `DashboardPage.tsx` + finální verify

**Files:**
- Modify: `src/pages/ikaros/DashboardPage.tsx`

- [ ] **Step 1: Najdi `--color-*` výskyt**

```bash
grep -n "color-" src/pages/ikaros/DashboardPage.tsx
```

- [ ] **Step 2: Oprav inline styly nebo CSS modul**

(Konkrétní oprava závisí na tom, jak je --color- použit; aplikuj mapping.)

- [ ] **Step 3: Final verify — žádný `--color-*` nikde v src/**

```bash
grep -rn "var(--color-" src/ --include="*.css" --include="*.tsx" --include="*.ts"
```
Expected: žádný výstup.

- [ ] **Step 4: Run full build + tests**

```bash
npm run build && npm run test:run
```
Expected: oba pass.

- [ ] **Step 5: Manuální cross-theme test**

```bash
npm run dev
```
- Otevři aplikaci
- Postupně přepni: Modré nebe → Bílá → Temná červeň
- V každém tématu klikni na různé stránky (Dashboard, Vesmíry, Profile)
- Žádné nečitelné texty, žádné neviditelné komponenty

- [ ] **Step 6: Commit**

```bash
git add src/pages/ikaros/DashboardPage.tsx
git commit -m "refactor: complete migration to theme tokens (no --color-* remaining)"
```

---

# M5: Storybook + theme decorator + axe checks

## Task 30b: Lint guard proti hardcoded barvám

**Files:**
- Create: `scripts/lint-no-hardcoded-colors.mjs`
- Modify: `package.json`

> Spec akceptační kritérium: "Žádná hardcoded barva v komponentě (ESLint pass)". Místo psaní custom ESLint pluginu (overhead) použijeme jednoduchý grep skript který CI může zavolat.

- [ ] **Step 1: Vytvoř `scripts/lint-no-hardcoded-colors.mjs`**

```js
import { readdir, readFile } from 'node:fs/promises';
import path from 'node:path';

const ROOT = path.resolve('src');
const ALLOW = [
  'src/themes/themes/',          // theme definice mají hexy
  'src/themes/_shared/',          // base reset/tokens
  'src/__tests__/',               // testy
  'src/themes/__tests__/',
];
const HEX_RE = /(?<!var\(--[\w-]*?:\s*?)#[0-9a-fA-F]{3,8}\b/g;
const RGB_RE = /\b(rgb|rgba|hsl|hsla)\s*\(/g;
const COLOR_NAME_RE = /\b(?:background|color|border)\s*:\s*(red|blue|green|black|white|gray|grey)\b/gi;

async function* walk(dir) {
  for (const entry of await readdir(dir, { withFileTypes: true })) {
    const p = path.join(dir, entry.name);
    if (entry.isDirectory()) {
      yield* walk(p);
    } else if (/\.(css|tsx?)$/.test(entry.name)) {
      yield p;
    }
  }
}

let violations = 0;

for await (const file of walk(ROOT)) {
  const rel = path.relative(process.cwd(), file).replace(/\\/g, '/');
  if (ALLOW.some((a) => rel.includes(a))) continue;

  const content = await readFile(file, 'utf8');
  const issues = [];
  for (const m of content.matchAll(HEX_RE)) issues.push(`hex ${m[0]}`);
  for (const m of content.matchAll(RGB_RE)) issues.push(`${m[1]}(...)`);
  for (const m of content.matchAll(COLOR_NAME_RE)) issues.push(`color name "${m[1]}"`);

  if (issues.length > 0) {
    console.error(`✗ ${rel}`);
    for (const issue of issues) console.error(`    ${issue}`);
    violations += issues.length;
  }
}

if (violations > 0) {
  console.error(`\nFound ${violations} hardcoded color(s). Use var(--token) instead.`);
  process.exit(1);
} else {
  console.log('✓ No hardcoded colors found in components.');
}
```

- [ ] **Step 2: Přidej skript do `package.json`**

```json
"lint:colors": "node scripts/lint-no-hardcoded-colors.mjs"
```

- [ ] **Step 3: Spusť skript**

```bash
npm run lint:colors
```
Expected: PASS — žádné hardcoded barvy v `src/components/`, `src/pages/`, `src/api/` atd.

- [ ] **Step 4: Pokud nějaké zbyly, oprav je**

(Migrace v M4 měla pokrýt vše, ale grep odhalí případně přehlédnuté hex hodnoty v inline stylech nebo TSX.)

- [ ] **Step 5: Commit**

```bash
git add scripts/lint-no-hardcoded-colors.mjs package.json
git commit -m "chore: add lint:colors guard against hardcoded colors"
```

---

## Task 31: Install Storybook

**Files:**
- Create: `.storybook/main.ts`
- Create: `.storybook/preview.tsx`

- [ ] **Step 1: Init Storybook**

```bash
npx storybook@latest init --type react --skip-install
npm i
```

> Storybook init by měl vytvořit `.storybook/` config a sample stories. Necháme jen config, sample stories smažeme.

- [ ] **Step 2: Smaž sample stories**

```bash
rm -rf src/stories
```

- [ ] **Step 3: Verify Storybook běží**

```bash
npm run storybook
```
Expected: otevře se na :6006, prázdný (žádné stories).

- [ ] **Step 4: Commit**

```bash
git add .storybook/ package.json package-lock.json
git commit -m "chore: install Storybook"
```

---

## Task 32: Theme decorator pro Storybook

**Files:**
- Modify: `.storybook/preview.tsx`
- Create: `.storybook/themeDecorator.tsx`

- [ ] **Step 1: Vytvoř `.storybook/themeDecorator.tsx`**

```tsx
import { useEffect } from 'react';
import type { Decorator } from '@storybook/react';
import { applyTheme } from '../src/themes/applyTheme';
import { listThemes } from '../src/themes/registry';
import '../src/index.css';

export const themeDecorator: Decorator = (Story, context) => {
  const themeId = (context.globals.theme as string) ?? 'modre-nebe';

  useEffect(() => {
    void applyTheme(themeId);
  }, [themeId]);

  return (
    <div data-theme={themeId} style={{ padding: 24, minHeight: '100vh', background: 'var(--bg-primary)' }}>
      <Story />
    </div>
  );
};

export const themeGlobalType = {
  theme: {
    name: 'Theme',
    description: 'Global theme for components',
    defaultValue: 'modre-nebe',
    toolbar: {
      icon: 'paintbrush',
      items: listThemes().map((t) => ({ value: t.id, title: t.name })),
      dynamicTitle: true,
    },
  },
};
```

- [ ] **Step 2: Aktualizuj `.storybook/preview.tsx`**

```tsx
import type { Preview } from '@storybook/react';
import { themeDecorator, themeGlobalType } from './themeDecorator';

const preview: Preview = {
  parameters: {
    backgrounds: { disable: true }, // řešíme tématem
  },
  globalTypes: themeGlobalType,
  decorators: [themeDecorator],
};

export default preview;
```

- [ ] **Step 3: Spusť Storybook a verify toolbar**

```bash
npm run storybook
```
- V toolbaru nahoře vidíš ikonu štětce s dropdownem "Modré nebe / Bílá / Temná červeň"

- [ ] **Step 4: Commit**

```bash
git add .storybook/
git commit -m "feat(storybook): theme decorator with all 3 themes"
```

---

## Task 33: Stories pro base UI komponenty

**Files:**
- Create: `src/components/ui/Button/Button.stories.tsx`
- Create: `src/components/ui/Card/Card.stories.tsx`
- Create: `src/components/ui/Input/Input.stories.tsx`
- Create: `src/components/ui/Badge/Badge.stories.tsx`

- [ ] **Step 1: Vytvoř `Button.stories.tsx`**

```tsx
import type { Meta, StoryObj } from '@storybook/react';
import { Button } from './Button';

const meta: Meta<typeof Button> = {
  title: 'UI/Button',
  component: Button,
};
export default meta;

type Story = StoryObj<typeof Button>;

export const Primary: Story = {
  args: { children: 'Klikni mě', variant: 'primary' },
};

export const Secondary: Story = {
  args: { children: 'Sekundární', variant: 'secondary' },
};

export const Disabled: Story = {
  args: { children: 'Vypnuto', disabled: true },
};
```

> **Pozn:** Pokud aktuální `Button` API se liší (např. nemá `variant`), uprav podle reálného propsu. Otevři `src/components/ui/Button/Button.tsx` a podívej se.

- [ ] **Step 2: Vytvoř `Card.stories.tsx`**

```tsx
import type { Meta, StoryObj } from '@storybook/react';
import { Card } from './Card';

const meta: Meta<typeof Card> = {
  title: 'UI/Card',
  component: Card,
};
export default meta;

type Story = StoryObj<typeof Card>;

export const Default: Story = {
  args: {
    children: (
      <>
        <h3>Nadpis karty</h3>
        <p>Toto je tělo karty s textem který by měl být čitelný ve všech tématech.</p>
      </>
    ),
  },
};
```

- [ ] **Step 3: Vytvoř `Input.stories.tsx`**

```tsx
import type { Meta, StoryObj } from '@storybook/react';
import { Input } from './Input';

const meta: Meta<typeof Input> = {
  title: 'UI/Input',
  component: Input,
};
export default meta;

type Story = StoryObj<typeof Input>;

export const Empty: Story = {
  args: { placeholder: 'Zadej text…' },
};

export const Filled: Story = {
  args: { defaultValue: 'Vyplněná hodnota' },
};
```

- [ ] **Step 4: Vytvoř `Badge.stories.tsx`**

```tsx
import type { Meta, StoryObj } from '@storybook/react';
import { Badge } from './Badge';

const meta: Meta<typeof Badge> = {
  title: 'UI/Badge',
  component: Badge,
};
export default meta;

type Story = StoryObj<typeof Badge>;

export const Default: Story = {
  args: { children: '5' },
};
```

- [ ] **Step 5: Verify v Storybook**

```bash
npm run storybook
```
- V sidebaru vidíš UI/Button, UI/Card, UI/Input, UI/Badge
- V toolbaru přepínej témata, komponenty se přebarvují
- Žádné výrazné contrast issues

- [ ] **Step 6: Commit**

```bash
git add src/components/ui/
git commit -m "feat(storybook): add stories for base UI components"
```

---

## Task 34: ThemeSwitcher story

**Files:**
- Create: `src/themes/ThemeSwitcher.stories.tsx`

- [ ] **Step 1: Vytvoř story**

```tsx
import type { Meta, StoryObj } from '@storybook/react';
import { ThemeSwitcher } from './ThemeSwitcher';

const meta: Meta<typeof ThemeSwitcher> = {
  title: 'Themes/ThemeSwitcher',
  component: ThemeSwitcher,
  parameters: {
    layout: 'centered',
  },
};
export default meta;

type Story = StoryObj<typeof ThemeSwitcher>;

export const Default: Story = {};
```

- [ ] **Step 2: Verify**

```bash
npm run storybook
```
- Themes/ThemeSwitcher zobrazí dropdown
- Klik → otevře, výběr funguje, mění globální theme decorator

- [ ] **Step 3: Commit**

```bash
git add src/themes/ThemeSwitcher.stories.tsx
git commit -m "feat(storybook): ThemeSwitcher story"
```

---

## Task 35: Axe accessibility addon

**Files:**
- Modify: `.storybook/main.ts`
- Modify: `.storybook/preview.tsx`

- [ ] **Step 1: Install addon**

```bash
npm i -D @storybook/addon-a11y
```

- [ ] **Step 2: Přidej do `.storybook/main.ts`**

V `addons` poli přidej:
```ts
addons: [
  '@storybook/addon-a11y',
  // ... existing addons
],
```

- [ ] **Step 3: Spusť Storybook a verify panel a11y**

```bash
npm run storybook
```
- Dole pod story se objeví záložka "Accessibility"
- Pro každou story projdi všechna 3 témata a zaznamenej případné contrast warnings

- [ ] **Step 4: Pokud `bila` má contrast issues v base komponentách, ladění**

(Závisí co axe najde — pravděpodobně bude třeba upravit barvy v `bila/index.ts` aby textový kontrast byl ≥ 4.5:1.)

- [ ] **Step 5: Commit**

```bash
git add .storybook/ package.json package-lock.json src/themes/themes/bila/index.ts
git commit -m "feat(storybook): add @storybook/addon-a11y + tweak bila for WCAG AA"
```

---

# M6: BE sync (volitelná část — graceful pokud BE chybí)

## Task 36: useThemeSync hook (TDD)

**Files:**
- Create: `src/themes/useThemeSync.ts`
- Create: `src/themes/__tests__/useThemeSync.test.tsx`

- [ ] **Step 1: Failing test**

`src/themes/__tests__/useThemeSync.test.tsx`:
```tsx
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { renderHook, waitFor } from '@testing-library/react';
import { Provider as JotaiProvider, createStore } from 'jotai';
import type { PropsWithChildren } from 'react';
import { useThemeSync } from '../useThemeSync';
import { themeAtom } from '../state';
import { currentUserAtom } from '../../store/authStore';
import { api } from '../../api/client';

vi.mock('../../api/client', () => ({
  api: {
    patch: vi.fn(() => Promise.resolve({})),
  },
  apiClient: {},
  parseApiError: vi.fn(() => 'err'),
}));

function makeWrapper(store = createStore()) {
  return ({ children }: PropsWithChildren) => (
    <JotaiProvider store={store}>{children}</JotaiProvider>
  );
}

describe('useThemeSync', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it('does NOT call API when user not logged in', () => {
    const store = createStore();
    store.set(themeAtom, 'bila');
    renderHook(() => useThemeSync(), { wrapper: makeWrapper(store) });
    expect(api.patch).not.toHaveBeenCalled();
  });

  it('calls PATCH /users/me when theme changes for logged-in user', async () => {
    const store = createStore();
    store.set(currentUserAtom, { id: '1', username: 'pj', themeId: 'modre-nebe' } as never);
    renderHook(() => useThemeSync(), { wrapper: makeWrapper(store) });

    store.set(themeAtom, 'temna-cerven');

    await waitFor(() => {
      expect(api.patch).toHaveBeenCalledWith('/users/me', { themeId: 'temna-cerven' });
    });
  });

  it('silently swallows 404 (BE endpoint missing)', async () => {
    (api.patch as ReturnType<typeof vi.fn>).mockRejectedValueOnce({ response: { status: 404 } });
    const consoleWarn = vi.spyOn(console, 'warn').mockImplementation(() => {});
    const store = createStore();
    store.set(currentUserAtom, { id: '1', username: 'pj' } as never);
    renderHook(() => useThemeSync(), { wrapper: makeWrapper(store) });
    store.set(themeAtom, 'bila');
    await waitFor(() => expect(api.patch).toHaveBeenCalled());
    // No throw, just warn
    expect(consoleWarn).toHaveBeenCalled();
    consoleWarn.mockRestore();
  });
});
```

- [ ] **Step 2: Run test — verify it fails**

```bash
npm run test:run -- useThemeSync.test
```
Expected: FAIL — `Cannot find module '../useThemeSync'`.

- [ ] **Step 3: Implement `src/themes/useThemeSync.ts`**

```ts
import { useEffect, useRef } from 'react';
import { useAtomValue } from 'jotai';
import { themeAtom } from './state';
import { currentUserAtom } from '../store/authStore';
import { api } from '../api/client';
import type { ThemeId } from './types';

export function useThemeSync(): void {
  const user = useAtomValue(currentUserAtom);
  const themeId = useAtomValue(themeAtom);
  const previous = useRef<ThemeId | null>(null);

  useEffect(() => {
    if (!user) {
      previous.current = themeId;
      return;
    }
    if (previous.current === null) {
      previous.current = themeId;
      return; // initial mount, neposílat
    }
    if (previous.current === themeId) return;
    previous.current = themeId;

    const handle = setTimeout(() => {
      api.patch('/users/me', { themeId }).catch((err: { response?: { status?: number } }) => {
        if (err?.response?.status === 404) {
          console.warn('[theme] BE sync skipped — endpoint /users/me theme not implemented yet');
          return;
        }
        console.warn('[theme] BE sync failed:', err);
      });
    }, 500);

    return () => clearTimeout(handle);
  }, [themeId, user]);
}
```

- [ ] **Step 4: Run test — verify it passes**

```bash
npm run test:run -- useThemeSync.test
```
Expected: PASS — všechny 3 testy.

- [ ] **Step 5: Commit**

```bash
git add src/themes/useThemeSync.ts src/themes/__tests__/useThemeSync.test.tsx
git commit -m "feat(themes): useThemeSync hook (BE sync, graceful 404)"
```

---

## Task 37: Wire useThemeSync v ThemeProvider

**Files:**
- Modify: `src/themes/ThemeProvider.tsx`

- [ ] **Step 1: Aktualizuj `ThemeProvider.tsx`**

```tsx
import { useEffect, type PropsWithChildren } from 'react';
import { useAtomValue } from 'jotai';
import { themeAtom } from './state';
import { applyTheme } from './applyTheme';
import { useThemeSync } from './useThemeSync';

export function ThemeProvider({ children }: PropsWithChildren) {
  const themeId = useAtomValue(themeAtom);

  useEffect(() => {
    void applyTheme(themeId);
  }, [themeId]);

  useThemeSync();

  return <>{children}</>;
}
```

- [ ] **Step 2: Manuální test**

```bash
npm run dev
```
- Login (pokud BE běží)
- Změň téma v switcheru → DevTools Network → vidíš `PATCH /api/users/me` s body `{ themeId: '...' }`
- Pokud BE vrátí 404, v console vidíš `[theme] BE sync skipped — endpoint /users/me theme not implemented yet`, ale UI funguje normálně

- [ ] **Step 3: Commit**

```bash
git add src/themes/ThemeProvider.tsx
git commit -m "feat(themes): wire useThemeSync into ThemeProvider"
```

---

## Task 38: Initial sync — BE → localStorage při loginu

**Files:**
- Modify: `src/themes/useThemeSync.ts`

- [ ] **Step 1: Rozšiř hook o initial sync**

Najdi blok `if (!user)` a přidej před něj logiku pro initial sync:
```ts
import { useEffect, useRef } from 'react';
import { useAtom, useAtomValue } from 'jotai';
import { themeAtom } from './state';
import { currentUserAtom } from '../store/authStore';
import { api } from '../api/client';
import type { ThemeId } from './types';

export function useThemeSync(): void {
  const user = useAtomValue(currentUserAtom);
  const [themeId, setThemeId] = useAtom(themeAtom);
  const previous = useRef<ThemeId | null>(null);
  const initialSynced = useRef(false);

  // Initial sync: pokud user má themeId a liší se od localStorage, BE výhrává
  useEffect(() => {
    if (!user) return;
    if (initialSynced.current) return;
    initialSynced.current = true;

    const userThemeId = (user as { themeId?: string }).themeId;
    if (userThemeId && userThemeId !== themeId) {
      setThemeId(userThemeId as ThemeId);
      previous.current = userThemeId as ThemeId;
    }
  }, [user, themeId, setThemeId]);

  // Outbound sync: na změnu posílá PATCH (debounced 500ms)
  useEffect(() => {
    if (!user) {
      previous.current = themeId;
      return;
    }
    if (previous.current === null) {
      previous.current = themeId;
      return;
    }
    if (previous.current === themeId) return;
    previous.current = themeId;

    const handle = setTimeout(() => {
      api.patch('/users/me', { themeId }).catch((err: { response?: { status?: number } }) => {
        if (err?.response?.status === 404) {
          console.warn('[theme] BE sync skipped — endpoint /users/me theme not implemented yet');
          return;
        }
        console.warn('[theme] BE sync failed:', err);
      });
    }, 500);

    return () => clearTimeout(handle);
  }, [themeId, user]);
}
```

- [ ] **Step 2: Manuální test**

(Předpokládá BE který vrací `user.themeId` v `/auth/me` — pokud ne, tato větev se neaktivuje.)

- [ ] **Step 3: Commit**

```bash
git add src/themes/useThemeSync.ts
git commit -m "feat(themes): initial BE → localStorage sync on login"
```

---

## Task 39: Použij dluh skill na BE-side závazky

- [ ] **Step 1: Aktivuj skill `dluh`**

Aktivuj skill a zaloguj následující dluhy:

1. **D-XXX**: BE endpoint `PATCH /api/users/me` neexistuje — FE volá fallbackem (404 swallowed). BE-tým přidá `themeId: string` field do User entity + DTO + endpoint.
2. **D-XXX**: `currentUserAtom` neobsahuje `themeId` field — po BE změně doplnit do `User` typu v `src/types/index.ts` aby initial sync v `useThemeSync` fungoval.
<!-- Původní třetí dluh (chybějící backgrounds) byl rozhodnut: PJ dodal všech 21 obrázků 2026-05-07. -->


> Skill `dluh` zapíše do `docs/dluhy.md` se správnými ID (D-NNN).

- [ ] **Step 2: Commit dluhy**

```bash
git add docs/dluhy.md
git commit -m "docs: log debt for BE theme sync + missing backgrounds"
```

---

# Final M-checkpoint

## Task 40: Iterace A finální verifikace

- [ ] **Step 1: Run all tests**

```bash
npm run test:run
```
Expected: všechny testy projdou (registry, applyTheme, useTheme, useThemeSync = ~14 tests).

- [ ] **Step 2: Run build**

```bash
npm run build
```
Expected: build success.

- [ ] **Step 3: Run lint**

```bash
npm run lint && npm run lint:colors
```
Expected: no errors, no hardcoded colors found.

- [ ] **Step 4: Manuální E2E checklist**

```bash
npm run dev
```
Otestuj v prohlížeči:
- [ ] První návštěva (smaž localStorage `ikaros.theme`) → výchozí téma `modre-nebe`
- [ ] Klik na ThemeSwitcher → otevře se dropdown s 3 tématy + thumbnaily
- [ ] Vyber `Bílá` → barvy se změní okamžitě, žádný flash
- [ ] Refresh stránky → `Bílá` zůstane (žádný FOUT)
- [ ] Klávesa Tab → fokus na switcher; Enter → otevře; ↓↑ → navigace; Enter → výběr; Esc → zavře
- [ ] Mobile (DevTools 375px) → otevře jako bottom-sheet
- [ ] V DevTools nastav `prefers-reduced-motion: reduce` → animace v `temna-cerven` (hover glow) zmizí
- [ ] Storybook (`npm run storybook`) → toolbar přepíná téma na všech storiích

- [ ] **Step 5: Push branch**

```bash
git push -u origin feat/theme-system-iterace-a
```

- [ ] **Step 6: Otevři PR (manuálně nebo přes `gh`)**

```bash
gh pr create --title "feat: Theme system Iterace A — foundation + 3 themes" --body "$(cat <<'EOF'
## Summary
- Theme infrastructure (registry, provider, switcher, applyTheme)
- 3 referenční témata: modre-nebe, bila, temna-cerven
- Hybrid persistence (localStorage + BE sync s graceful 404)
- Storybook s theme decoratorem
- Vitest setup + 14 tests
- Pre-hydration script (no FOUT)
- Architecture připravená pro fázi 5.0 (theme scope na úrovni layoutu)

## Spec
docs/superpowers/specs/2026-05-07-theme-system-design.md

## Plan
docs/superpowers/plans/2026-05-07-theme-system-iterace-a.md

## Test plan
- [ ] Vitest: `npm run test:run` (14 tests pass)
- [ ] Build: `npm run build`
- [ ] Storybook: `npm run storybook` (toolbar přepíná všechny 3 témata)
- [ ] E2E: výběr tématu, refresh, mobile bottom-sheet, prefers-reduced-motion

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

---

## Co dál (Iterace B — samostatný plán)

Po merge této PR:
1. PJ dodá zbývající background obrázky
2. Spustí se nový writing-plans cyklus pro Iteraci B = zbývajících 18 témat
3. Iterace B využije šablonu `<id>/index.ts` + `decorations.css` z této iterace
4. Visual regression setup (Chromatic) přidává Iterace B finální milestone
